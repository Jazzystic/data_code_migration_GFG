# PostgreSQL connection
# Copy to .env and fill with your own credentials — never commit .env
PGHOST=your-server.postgres.database.azure.com
PGDATABASE=your_database
PGUSER=your_user
PGPASSWORD=your_password
